#include <stdio.h>

int main(int argc, char *argv[])
{      
       int a[4];

       a[0] = 1;
       a[1] = 1;
       a[2] = 2;
       a[3] = 3;

       printf("Recognize this serie: %d, %d, %d, %d\n", a[0], a[1], a[2], a[3]);

       return 0;

}
